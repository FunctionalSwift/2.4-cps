//: Playground - Continuation Passing Style

class Model {
    func count() -> Int {
        return 0
    }
}

let model = Model()

if model.count() == 0 {
    //no data
} else {
    //we have data
}
